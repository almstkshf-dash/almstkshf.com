[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  SECRETS_SCAN_OMIT_KEYS = "CLERK_SECRET_KEY,NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY"
  SECRETS_SCAN_OMIT_PATHS = ".netlify/functions-internal/**,.next/server/**,node_modules/**"
